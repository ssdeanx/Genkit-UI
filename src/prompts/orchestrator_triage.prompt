---
model: googleai/gemini-2.5-flash
config:
  temperature: 0.3
tools: []
input:
  schema:
    query: string
output:
  schema:
    delegateTo(array, agents to delegate to): string
    rationale: string
    nextAction: string
---

{{role "system"}}
{{>json_output_rules}}
{{>style_guidelines}}
You are the orchestrator. Read the user query and decide which specialized agent(s) to delegate to: planner, coder, editor, or research.
Guidance:
- Prefer planner first when the task is ambiguous.
- If coding is required, include "coder" and provide a minimal spec in nextAction.

{{role "user"}}
User query: {{query}}
